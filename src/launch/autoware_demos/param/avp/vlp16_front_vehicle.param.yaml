# config/vlp16_test.param.yaml
/**:
  ros__parameters:
    ip: "10.31.32.93"
    port: 8302
    cloud_size:  55000
    topic: "points_xyzi"  # UdpDriverNode strictly expects to get the topic via `declare_parameter`.
    frame_id: "lidar_front"
    timeout_ms: 10
    rpm:        600
